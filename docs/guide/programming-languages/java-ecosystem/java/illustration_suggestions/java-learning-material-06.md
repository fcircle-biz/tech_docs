# 図解提案レポート: java-learning-material-06.html

生成日時: 2026-01-04
対象ファイル: /home/ichimaru/git/tech_docs/docs/guide/programming-languages/java-ecosystem/java/java-learning-material-06.html

---

| セクション / コンテキスト | 提案する図解 | 図解タイプ | 優先度 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| 6.1 メソッドって何だろう？ | メソッドの再利用性を示す概念図 | 概念図 | 中 | 同じメソッドを複数箇所から呼び出す様子を視覚化（1つの定義→複数の呼び出し） |
| 6.3 メソッドを作ってみよう | 既存Mermaid図の補完図 | フローチャート | 低 | 既存のメソッド構造図で十分説明されている |
| 6.4 引数を受け取るメソッド | 引数の値渡しの仕組み | シーケンス図 | 高 | メソッド呼び出し時に引数がどのようにコピーされて渡されるかを段階的に示す |
| 6.5 戻り値を返すメソッド | 戻り値のメモリ上の動き | シーケンス図 | 高 | メソッド内で計算した値が呼び出し元に返される流れを視覚化 |
| 6.6 メソッドのオーバーロード | 同名メソッドの引数による区別の仕組み | 概念図 | 中 | add(int a, int b)とadd(int a, int b, int c)がどのように区別されるかを示す |
| スコープの概念 | ローカル変数のスコープ範囲 | スコープ図 | 高 | メソッド内で宣言された変数の有効範囲をブロック単位で視覚化 |

---

## 優先度別サマリー

### 高優先度
- **6.4 引数の値渡し**: メソッド呼び出し時に引数の値がコピーされて渡される仕組みを段階的に示すシーケンス図
- **6.5 戻り値の動き**: メソッド内の計算結果が呼び出し元に返される流れを示すシーケンス図
- **スコープの概念**: ローカル変数の有効範囲をブロック単位で示すスコープ図

### 中優先度
- **6.1 メソッドの再利用性**: 1つのメソッド定義から複数箇所で呼び出せる様子を示す概念図
- **6.6 メソッドのオーバーロード**: 同名メソッドが引数の型・個数で区別される仕組みを示す図

### 低優先度
- **6.3 メソッド構造図**: 既存Mermaidダイアグラムで十分説明されている
